// import { useParams } from 'react-router-dom';
// import usePokemonDetail from '../hooks/usePokemonDetail';

export const DetailPage = () => {
  // const { id } = useParams();
  // const { pokemon, isPending, isLoading } = usePokemonDetail(id!);

  return <></>;
};

export default DetailPage;
